# GitHub Setup Instructions

Harris needs to do these manually (GitHub API requires auth):

1. Go to: https://github.com/organizations/plan
2. Click "Create a free organization"
3. Organization name: `verdictos`
4. Contact email: admin@verdictos.tech
5. This organization belongs to: My personal account
6. Click "Next"

Then create repository:
1. Click "Create a new repository"
2. Repository name: `scan`
3. Description: "Security scanner for AI-generated code"
4. Public
5. Do NOT initialize (we have code already)
6. Click "Create repository"

Then run these commands:
```bash
cd /data/.openclaw/workspace/verdictos-scan
git remote add origin https://github.com/verdictos/scan.git
git branch -M main
git push -u origin main
```

I'll handle the rest once that's done!
